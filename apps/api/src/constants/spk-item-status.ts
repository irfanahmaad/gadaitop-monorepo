/**
 * SPK item lifecycle status (storage / auction / sold / returned).
 */
export enum SpkItemStatusEnum {
  InStorage = 'in_storage',
  InAuction = 'in_auction',
  Sold = 'sold',
  Returned = 'returned',
}
